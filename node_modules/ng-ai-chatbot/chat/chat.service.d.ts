import { CoreService } from '../core/core.service';
import { HttpClient } from '@angular/common/http';
export declare class ChatService extends CoreService {
    http: HttpClient;
    constructor(http: HttpClient);
    send(intent: any, botId?: string): Promise<{}>;
}
